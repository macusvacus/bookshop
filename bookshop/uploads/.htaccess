# Prevent any uploaded file from ever being executed as PHP.
# Without this, someone could rename a malicious script to book_cover.jpg.php-like
# trick and have the server run it. Images should only ever be served, not executed.
<FilesMatch "\.(php|phtml|php3|php4|php5|phar)$">
    Require all denied
</FilesMatch>
